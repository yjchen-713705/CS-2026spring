# 计算机组成原理实验报告

## 基本信息
- 实验名称：Lab1-2
- 姓名：陈一璟
- 学号：24300120183

## 一、实验目的
TODO：在这里讲人话
1. 绘制完整状态转移图，表示1011检测模块的状态转移；
2. 设计自动机检测1011序列，补全代码；
3. 运行Vivado仿真，并加以说明。

## 二、实验原理
1. 自动机原理
  - 状态：状态机的当前状态，用于表示当前的输入和输出。
  - 输入：状态机的输入，用于触发状态转换。
  - 输出：状态机的输出，用于表示当前状态的结果。
  - 状态转换：根据当前状态和输入，将状态转换为下一个状态。

TODO：还能编点啥上去我不知道

## 三、实验步骤
### 1. 关键代码
1. Onehot 编码补全
```verilog
parameter IDLE = 5'b00001,    // 空闲状态  
          S1   = 5'b00010,    // 检测到1
          S2   = 5'b00100,    // 检测到10
          S3   = 5'b01000,    // 检测到101
          S4   = 5'b10000;    // 检测到1011（目标序列）  
```
2. 状态转移逻辑
```verilog
always@(posedge Clk_50 or negedge Rst_n)     // 在上升沿或复位时更新状态
begin  
    if (!Rst_n) begin
        state <= IDLE;  // 复位时，状态为空闲状态
    end else begin
        case(state)     // 根据当前状态和输入数据更新状态
            IDLE: if (data_in)    // IDLE状态下输入为1时，状态转换为S1
                    state <= S1;
                else
                    state <= IDLE;
            S1: if (!data_in)    // S1状态下输入为0时，状态转换为S2
                    state <= S2;
                else
                    state <= S1;
            S2: if (data_in)     // S2状态下输入为1时，状态转换为S3
                    state <= S3;
                else
                    state <= IDLE;
            S3: if (data_in)     // S3状态下输入为1时，状态转换为S4，此时探测到1011序列
                    state <= S4;
                else
                    state <= S2;
            S4: if (data_in)
                    state <= S1;
                else
                    state <= S2;
            default: state <= IDLE;
        endcase
    end
end  
```

3. 输出逻辑
```verilog
// 当且仅当检测到目标序列1011时输出高电平 ，其余时刻输出低电平
always@(*)  
begin  
    if (state == S4)
        data_out = 1'b1;
    else
        data_out = 1'b0;
end  
```

## 四、实验结果

### 1. 完整状态转移图
![状态转移图](pics\statechart.jpg)
- 其中，`Idle`状态为初始状态，蓝色为一般状态，红色为最终可接受状态（探测到`1011`序列）。

### 2. 波形结果（适用于Vivado仿真）
![波形结果](pics\sim_wave.png)
- `Clk_50`为50MHz时钟信号，`Rst_n`为复位信号，`data_in`为输入数据`为输出数据。

## 五、实验思考
### 1. 遇到的问题及解决方法
1. 问题描述：
   解决方法：

2. 问题描述：
   解决方法：

### 2. 实验心得

（描述通过本次实验学到的知识和技能）

## 六、实验评价
### 1. 自我评价

- 实验完成度：□优秀 □良好 □一般 □待提高
- 掌握程度：□很好 □较好 □一般 □需要加强

### 2. 实验反馈
1. 实验内容难度：□偏难 □适中 □偏易
3. 实验时间安排：□充足 □适中 □紧张

### 3. 建议与改进（可选）

